NotificationService
 ├── PortalNotificationBackend        (Flatpak)
 └── FreedesktopNotificationBackend   (Desktop normal)


Flatpak → usar portal → PortalNotificationBackend (Flatpak)

Desktop normal → usar org.freedesktop.Notifications → FreedesktopNotificationBackend (Desktop normal)